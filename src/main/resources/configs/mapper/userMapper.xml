<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fenfei.ssh.dao.IUserDAO">

    <select id="getUser"
            parameterType="int" resultType="com.fenfei.ssh.pojos.User">
        SELECT * FROM tb_user WHERE id=#{uid}
    </select>

    <update id="updateUser" parameterType="com.fenfei.ssh.pojos.User">
        UPDATE tb_user SET username = #{username} WHERE id = #{uid}
    </update>

    <insert id="addUser" parameterType="com.fenfei.ssh.pojos.User">
        INSERT INTO tb_user (username , password) VALUE ( #{username} , #{password});
    </insert>

    <delete id="deleteUser" parameterType="int">
        DELETE from tb_user WHERE id= #{uId}
    </delete>

    <!--查询所有的用户-->
    <resultMap id="AllUser" type="com.fenfei.ssh.pojos.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="phone" property="phone"/>
        <result column="major" property="major"/>
        <result column="sex" property="sex"/>
    </resultMap>

    <select id="getAllUsers" resultMap="AllUser">
        SELECT * from tb_user;
    </select>


</mapper>